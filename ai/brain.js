const MapResolution = [82, 50]

function MapImage(Image, Resolution){
    const newImage = new Array(MapResolution[0] * MapResolution[1]);

    
}

function CalculateActions(Environment, Image, Resolution){
    const ImageMapped = MapImage(Image, Resolution)
}

return CalculateActions